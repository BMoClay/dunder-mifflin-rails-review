<% @employee.attributes.each do |attribute, value| %>
  <p><%= attribute.to_s.titleize %>: <%= value %></p>
<% end %>

<h2>Employees:</h2>

<% @employees.each do |emp| %>
  <p><%= emp.first_name %></p>
<% end %>

<%= link_to "Back to all", employees_path %>